module.exports = {
  fn: (main, oldMember, newMember) => {
    // console.log(main.api.users.get(newMember.id).presence.status);
    // console.log(oldMember.presence.status);
    // console.log(newMember.presence.status);
    // console.log(newMember.presence.presenceUpdated);
    // newMember.presence.presenceUpdated = true;
    //
    //
    // console.log('---');
  },
};
